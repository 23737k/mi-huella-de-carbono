spring:
  datasource:
      url: jdbc:mysql://localhost:3306/mi-huella-pruebas
      username: root
      password:
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
logging:
  level:
    org:
      springframework:
        web:
          debug
      hibernate:
        error