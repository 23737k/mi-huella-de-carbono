spring:
  application:
    name: curso-spring
#Aqui seteo el perfil que utilizará Spring (yml). Lo que hara sera cargar las propiedades de este archivo y luego sobreescribira aquellas que aparezcan en el perfil activo
  profiles:
    active: local
  mvc:
    view:
      suffix: .jsp
      prefix: /WEB-INF/jsp/
  datasource:
    url: jdbc:mysql://sql10.freesqldatabase.com:3306/sql10699337
    username: sql10699337
    password: MLeR2Lz8Ii
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
server:
  port: 80

